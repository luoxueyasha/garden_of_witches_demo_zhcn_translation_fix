name,ko,#추가설명,en,ja,zh-cn,zh-tw,de,it,es-eu,fr
Player.Active.Weapon.Scissor,`가위`로 적을 공격합니다. 세 번째 공격은 `치명타`로 적중하며 피해량이 `{$ScissorCritical:0.##%}` 증가합니다.,,"Attacks the enemy with `Scissors`. The third attack inflicts a `Critical Hit`, increasing damage by `{$ScissorCritical:0.##%}`.",`ハサミ`で敵を攻撃します。3回目の攻撃は`クリティカル`で的中し、ダメージ量が`{$ScissorCritical:0.##%}`増加します。,使用`剪刀`攻击敌人。第三次攻击将造成`暴击`，增加`{$ScissorCritical:0.##%}`伤害。,以`剪刀`攻擊敵人。 第三次攻擊造成`暴擊`，傷害量增加`{$ScissorCritical:0.##%}`。,Greift den Gegner mit einer `Schere` an. Der dritte Angriff führt zu einem `kritischen Treffer` und erhöht den Schaden um `{$ScissorCritical:0.##%}`.,"Attacca il nemico con le `Forbici`. Il terzo attacco infligge un `colpo critico`, aumentando i danni del `{$ScissorCritical:0.##%}`.",Ataca al enemigo con las `tijeras`. El tercer ataque inflige un `golpe crítico` que aumenta el daño en un `{$ScissorCritical:0.##%}`.,Attaque l'ennemi avec des `ciseaux`. La troisième attaque inflige un `coup critique` et augmente les dégâts infligés de `{$ScissorCritical:0.##%}`.
Player.Active.Evade.Dash,원하는 방향으로 빠르게 `{$DashDistance}m` 이동합니다.,,Quickly moves `{$DashDistance}m` in the desired direction.,移動したい方向に素早く`{$DashDistance}m`移動します。,向对应方向快速移动`{$DashDistance}米`。,朝希望的方向快速移動`{$DashDistance}m`。,Bewegt sich schnell `{$DashDistance}m` in die gewünschte Richtung.,Si sposta rapidamente di `{$DashDistance}m` nella direzione scelta.,Se mueve velozmente `{$DashDistance} m` en la dirección deseada.,Se déplace rapidement sur `{$DashDistance} m` dans la direction sélectionnée.
Player.Active.Magic.ReaperSwing,`{$Radius}m` 이내의 모든 적을 공격합니다.,,Attacks all enemies within `{$Radius}m`.,`{$Radius}m`以内にいるすべての敵を攻撃します。,攻击`{$Radius}米`内的所有敌人。,攻擊`{$Radius}m`內的所有敵人。,Greift alle Gegner im Umkreis von `{$Radius}m` an.,Attacca tutti i nemici entro `{$Radius}m`.,Ataca a todos los enemigos en un radio de `{$Radius} m`.,Attaque tous les ennemis dans un rayon de `{$Radius} m`.
Player.Active.Magic.Ambush,"가장 가까운 적의 등 뒤로 이동한 후, `{$AttackRange}m` 이내의 모든 적을 공격하고 `가위 강화`를 `{$WeaponEnhancementStack}%` 획득합니다.",,"Moves behind the nearest enemy, before attacking all enemies within `{$AttackRange}m` and gaining `{$WeaponEnhancementStack}%` `Whetstone`.",最も近い敵の背後に移動した後、`{$AttackRange}m`以内にいるすべての敵を攻撃し、`ハサミ強化`を`{$WeaponEnhancementStack}%`獲得します。,快速移动至最近的敌人身后，攻击`{$AttackRange}米`内的所有敌人，并获得`{$WeaponEnhancementStack}%``剪刀强化`。,快速移動至最近的敵人身後， 攻擊`{$AttackRange}m`內的所有敵人，並獲得`{$WeaponEnhancementStack}%``強化剪刀`。,"Bewegt sich hinter den nächsten Gegner, und greift dann alle Gegner im Umkreis von `{$AttackRange}m` an und erhält `{$WeaponEnhancementStack}%` `Wetzstein`.","Si sposta dietro il nemico più vicino, prima di attaccare tutti i nemici entro `{$AttackRange}m` e guadagnare `{$WeaponEnhancementStack}%` `Cote`.","Se coloca detrás del enemigo más cercano, antes de atacar a todos los enemigos en un radio de `{$AttackRange} m` y obtener `{$WeaponEnhancementStack}%` `piedra(s) de afilar`.","Se déplace derrière l'ennemi le plus proche et attaque tous les ennemis dans un rayon de `{$AttackRange} m`, puis reçoit `{$WeaponEnhancementStack}%` `pierres à aiguiser`."
Player.Active.Magic.CounterAttack,"`{$BlockDuration}초`간 대부분의 피해를 막습니다. 적의 공격을 막거나 효과가 종료되면, `{$AttackRange}m` 이내의 모든 적을 공격합니다. `{$PerfectBlockDuration}초` 이내에 피해를 막으면 `반격`의 피해량이 `{$MoreDamageOnPerfectBlock:0.##%}`증가하고, 쿨타임을 `{$CooltimeDecrOnPerfectBlock}초` 회복합니다.",스킬 매커니즘 변경,"Blocks most damage for `{$BlockDuration}s`. When an enemy attack is blocked or the effect ends, attack all enemies within `{$AttackRange}m`. If damage is blocked within `{$PerfectBlockDuration}s`, `Counterattacks` inflict `{$MoreDamageOnPerfectBlock:0.##%}` more damage and recovers `{$CooltimeDecrOnPerfectBlock}s` of cooldown.",`{$BlockDuration}秒間`ほとんどのダメージを防ぎます。敵の攻撃を防ぐか効果が終了すると、`{$AttackRange}m`以内の敵全員を攻撃します。`{$PerfectBlockDuration}秒`以内にダメージを防ぐと、`反撃`のダメージ量が`{$MoreDamageOnPerfectBlock:0.##%}`増加し、クールタイムが`{$CooltimeDecrOnPerfectBlock}秒`回復します。,在`{$BlockDuration}秒`内格挡大部分伤害。若成功格挡敌人攻击或效果结束，则攻击`{$AttackRange}米`内的所有敌人。若在`{$PerfectBlockDuration}秒`内格挡了伤害，`反击`的伤害增加`{$MoreDamageOnPerfectBlock:0.##%}`并恢复`{$CooltimeDecrOnPerfectBlock}秒`冷却时间。,`{$BlockDuration}秒`內阻擋大部分傷害。成功阻擋敵人攻擊或效果結束時，攻擊`{$AttackRange}m`內的所有敵人。`{$PerfectBlockDuration}秒`內成功阻擋傷害時，`反擊`傷害量增加`{$MoreDamageOnPerfectBlock:0.##%}`，冷卻時間減少`{$CooltimeDecrOnPerfectBlock}秒`。,"Blockt den meisten Schaden für `{$BlockDuration}s`. Wenn ein gegnerischer Angriff geblockt wird oder der Effekt endet, greifst du alle Gegner innerhalb von `{$AttackRange}m` an. Wenn Schaden innerhalb von `{$PerfectBlockDuration}s` geblockt wird, fügen `Gegenangriffe` `{$MoreDamageOnPerfectBlock:0.##%}` mehr Schaden zu und erholen `{$CooltimeDecrOnPerfectBlock}s` der Abklingzeit.","Blocca la maggior parte dei danni per `{$BlockDuration}s`. Al termine dell'effetto, o quando un attacco nemico viene bloccato, attacca tutti i nemici nel raggio di `{$AttackRange}m`. Se i danni vengono bloccati entro `{$PerfectBlockDuration}s`, i `contrattacchi` infliggono `{$MoreDamageOnPerfectBlock:0.##%}` danni aggiuntivi e ripristinano `{$CooltimeDecrOnPerfectBlock}s` del tempo di recupero.","Bloquea la mayor parte del daño durante`{$BlockDuration}s`. Cuando se bloquea un ataque enemigo o termina el efecto, ataca a todos los enemigos en un radio de `{$AttackRange}m`. Si se bloquea el daño en menos de `{$PerfectBlockDuration} s`, Los `contraataques` infligen un `{$MoreDamageOnPerfectBlock:0.##%}` más de daño y se reduce(n) `{$CooltimeDecrOnPerfectBlock}s` de tiempo de recuperación.","Bloque la plupart des attaques pendant `{$BlockDuration} s`. Lorsqu'une attaque ennemie est bloquée ou que l'effet prend fin, attaque tous les ennemis dans un rayon de `{$AttackRange} m`. Si des dégâts sont bloqués sous `{$PerfectBlockDuration} s`, les `contre-attaques` infligent `{$MoreDamageOnPerfectBlock:0.##%}` de dégâts supplémentaires et le temps de recharge est réduit de `{$CooltimeDecrOnPerfectBlock} s`."
Player.Active.Magic.SpawnFamiliar,적을 `{$AttackCount}회` 공격하는 작은 `인형` `{$SpawnCount}개`를 소환합니다.,,Summons `{$SpawnCount}` small `Doll(s)` to attack enemies `{$AttackCount} times`.,敵を`{$AttackCount}回`攻撃する小さな`人形`を`{$SpawnCount}体`召喚します。,召唤`{$SpawnCount}只`能够攻击敌人`{$AttackCount}次`的小型`人偶`。,召喚`{$SpawnCount}個`會攻擊敵人`{$AttackCount}次`的小型`玩偶`。,"Beschwört `{$SpawnCount}` kleine `Puppe(n)`, um Gegner `{$AttackCount} Mal` anzugreifen.",Evoca `{$SpawnCount}` piccola/e `Bambola/e` per attaccare i nemici `{$AttackCount} volte`.,Invoca `{$SpawnCount}` `muñeco(s) pequeño(s)` que ataca(n) a los enemigos `{$AttackCount} veces`.,Invoque `{$SpawnCount}` petite(s) `poupée(s)` qui attaquent les ennemis `{$AttackCount} fois`.
Player.Active.Magic.SpawnTrap,적을 추적하며 지속적으로 공격하는 `물레바퀴`를 내 근처에 소환합니다. `물레바퀴`는 `{$TrapStayTime}초` 뒤 나에게로 돌아옵니다.,,Summons a `Sewing Wheel` nearby that chases and continuously attacks enemies. `Sewing Wheel` returns to caster after `{$TrapStayTime}s`.,敵を追跡し、持続的に攻撃する`糸車`を自分の周りに召喚します。`糸車`は、`{$TrapStayTime}秒`後に自分の元へ戻ってきます。,召唤能够持续追踪并攻击敌人的`纺车`。`纺车`会在`{$TrapStayTime}秒`后返回。,在自己附近召喚會追蹤敵人並持續攻擊的`紡車`。 `紡車`會在`{$TrapStayTime}秒`後返回身邊。,"Beschwört ein Spinnrad in der Nähe, das Gegner jagt und kontinuierlich angreift. Das Spinnrad kehrt nach {$TrapStayTime}s zum Wirkenden zurück.",Evoca una Ruota dentata nelle vicinanze che insegue e attacca continuamente i nemici. La Ruota dentata ritorna dopo {$TrapStayTime}s.,Invoca una rueda giratoria cerca que persigue y ataca sin parar a los enemigos. La rueda giratoria vuelve a quien la lanzo transcurridos {$TrapStayTime} s.,Invoque un rouet qui poursuit et attaque continuellement les ennemis. Le rouet retourne auprès du lanceur après {$TrapStayTime} s.
Player.Active.Magic.ShotNeedle,"`{$SearchRange}m` 이내의 모든 적에게 `바늘`을 `{$NeedleCount}개` 발사하고, `{$AttackDelay}초` 뒤 `실`로 연결된 모든 대상에게 피해를 줍니다. 대상에게 연결된 `실` `1개`당 피해량이 `{$NeedleDamageIncreaseRate:0.##%}` 증가합니다.",,"Shoots `{$NeedleCount}` `Needle(s)` at all enemies within `{$SearchRange}m`. After `{$AttackDelay}s`, inflicts damage on all targets connected by `Thread`. Damage increases by `{$NeedleDamageIncreaseRate:0.##%}` per `Thread`.",`{$SearchRange}m`以内にいるすべての敵に`針`を`{$NeedleCount}つ`発射し、`{$AttackDelay}秒`後に`糸`でつながれたすべての対象にダメージを与えます。対象につながれた`糸``1つ`につきダメージ量が`{$NeedleDamageIncreaseRate:0.##%}`増加します。,向`{$SearchRange}m`内的所有敌人发射`{$NeedleCount}个``缝针`。`丝线`连接的所有目标将在`{$AttackDelay}秒`后受到伤害。每`1个`连接目标会使`丝线`增加`{$NeedleDamageIncreaseRate:0.##%}`伤害。,向`{$SearchRange}m`內的所有敵人發射`{$NeedleCount}根``細針`， `{$AttackDelay}秒`後，對以`線`連接的所有目標造成傷害。 每`1條`連接目標的`線`，傷害量增加`{$NeedleDamageIncreaseRate:0.##%}`。,"Schießt `{$NeedleCount}` `Nadel(n)` auf alle Gegner im Umkreis von `{$SearchRange}m`. Fügt nach `{$AttackDelay}s` allen Zielen, die durch einen `Faden` verbunden sind, Schaden zu. Der Schaden erhöht sich pro `Faden` um `{$NeedleDamageIncreaseRate:0.##%}`.","Scaglia `{$NeedleCount}` `Ago/i` a tutti i nemici entro `{$SearchRange}m`. Dopo `{$AttackDelay}s`, infligge danni a tutti i bersagli collegati da `Filo`. I danni aumentano del `{$NeedleDamageIncreaseRate:0.##%}` per ogni `Filo`.","Dispara `{$NeedleCount}` `aguja(s)` a los enemigos en un radio de `{$SearchRange} m`. Transcurridos `{$AttackDelay} s`, inflige daño a todos los enemigos unidos por el `hilo`. El daño aumenta un `{$NeedleDamageIncreaseRate:0.##%}` por `hilo`.","Tire `{$NeedleCount}` `aiguille(s)` sur tous les ennemis dans un rayon de `{$SearchRange} m`. Après `{$AttackDelay} s`, inflige des dégâts à toutes les cibles reliées par un `fil`. Les dégâts augmentent de `{$NeedleDamageIncreaseRate:0.##%}` par `fil`."
Player.Active.Magic.Boost,"`{$Duration}초`간 이동속도가 `{$Boost.MoveSpeed.Incr:0.##%}` 증가되며, 지속시간 동안 `게이지`가 최대 `{$MaxSeedGauge}%`까지 충전됩니다. 효과가 종료되면 `{$DamageRadius}m` 이내의 모든 적에게 피해를 입히고, 충전된 `게이지` `{$GaugePerSeed}%`당 `씨앗`을 `1개` 생성합니다. 피해량은 충전된 `게이지`만큼 증가됩니다.",스킬 설명 좀더 구체적으로 변경,"Increases Move SPD by `{$Boost.MoveSpeed.Incr:0.##%}` for `{$Duration}s`, and charges `Gauge` up to `{$MaxSeedGauge}%` for the duration. When the effect ends, inflicts damage on all enemies within `{$DamageRadius}m` and summons `1` `seed` per `{$GaugePerSeed}%` of `Gauge` charge. Damage is increased by the amount of charged `Gauge`.",`{$Duration}秒`間、移動速度が`{$Boost.MoveSpeed.Incr:0.##%}`増加し、持続時間中は`ゲージ`が最大`{$MaxSeedGauge}%`までチャージされます。効果終了時、`{$DamageRadius}m`以内の敵全員にダメージを与えてチャージされた`ゲージ` `{$GaugePerSeed}%`につき`種`を`1つ`生成します。チャージされた`ゲージ`分ダメージ量が増加します。,`{$Duration}秒`内移动速度增加`{$Boost.MoveSpeed.Incr:0.##%}`，持续时间内`计量条`最多可充能至`{$MaxSeedGauge}%`。效果结束后，对`{$DamageRadius}米`内的所有敌人造成伤害，且充能`计量条`的每`{$GaugePerSeed}%`生成`1个``种子`。伤害会随着所充能`计量条`的增加而增加。,`{$Duration}秒`內移動速度增加`{$Boost.MoveSpeed.Incr:0.##%}`，持續期間內，`計量條`最多蓄能至`{$MaxSeedGauge}%`。效果結束後，對`{$DamageRadius}m`內的所有敵人造成傷害，蓄能後的`計量條`每`{$GaugePerSeed}%`生成`1個``種子`。傷害量依蓄能的`計量條`比例增加。,"Erhöht das Bewegungs-TMP um `{$Boost.MoveSpeed.Incr:0.##%}` für `{$Duration}s` und lädt den `Balken` bis zu `{$MaxSeedGauge}%` für die Dauer. Wenn der Effekt endet, fügt er allen Gegnern innerhalb von  {$DamageRadius}m` Schaden zu und beschwört `1` `Samen` pro `{$GaugePerSeed}%` der Ladung des `Balkens`. Der Schaden erhöht sich um die Menge des geladenen `Balken`.","Aumenta la VEL. di movimento di `{$Boost.MoveSpeed.Incr:0.##%}` per `{$Duration}s` e carica la `barra` fino al `{$MaxSeedGauge}%` per la durata. Al termine dell'effetto, infligge danni a tutti i nemici nel raggio di `{$DamageRadius}m` ed evoca `1` `seme` ogni `{$GaugePerSeed}%` della carica della `barra`. I danni aumentano in base alla quantità di `barra` caricata.","Aumenta la VEL de movimiento un  `{$Boost.MoveSpeed.Incr:0.##%}` durante `{$Duration}s`, y carga el `medidor` hasta un `{$MaxSeedGauge}%` mientras dura. Al terminar el efecto, inflige daño a todos los enemigos en un radio de `{$DamageRadius}m` e invoca `1` `semilla` por cada `{$GaugePerSeed}%` de carga del `medidor`. El daño aumenta en función de la cantidad de `medidor` cargado.","Augmente la VIT de déplacement de `{$Boost.MoveSpeed.Incr:0.##%}` pendant `{$Duration} s`, et charge la `jauge` jusqu'à `{$MaxSeedGauge} %` pour la durée de l'effet. Lorsque l'effet prend fin, inflige des dégâts à tous les ennemis dans un rayon de `{$DamageRadius} m` et invoque `1` `graine` pour chaque `{$GaugePerSeed} %` de charge de `jauge`. Les dégâts augmentent selon la quantité de charge de `jauge`."
Player.Active.Magic.SpawnCrow,`어둠의 기운`을 가장 가까운 적을 향해 날려 피해를 주고 `까마귀`를 `{$SpawnCount}마리` 소환합니다.,,"Fires `Shadow Surge` at the nearest enemy to inflict damage, and summons `{$SpawnCount}` `Crow(s)`.",`闇のオーラ`を最も近い敵に向かって放ちダメージを与え、`鴉`を`{$SpawnCount}羽`召喚します。,向最近的敌人发射`暗黑气息`造成伤害，并召唤`{$SpawnCount}只``乌鸦`。,向最近的敵人發射`暗黑氣息`並造成傷害後， 召喚`{$SpawnCount}隻``烏鴉`。,"Feuert `dunkle Energie` auf den nächstgelegenen Gegner, um ihm Schaden zuzufügen und beschwört `{$SpawnCount}` `Krähe(n)`.","Diffonde `Energia oscura` al nemico più vicino per infliggere danni, ed evoca `{$SpawnCount}` `Corvo/i`.",Dispara `energía oscura` al enemigo más cercano para infligirle daño e invoca `{$SpawnCount}` `cuervo(s)`.,"Projette de `l'énergie sombre` sur l'ennemi le plus proche, lui infligeant des dégâts et invoquant `{$SpawnCount}` `corbeau(x)`."